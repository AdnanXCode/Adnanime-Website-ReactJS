const Header = ({title}) => {
    return (
        <div className="p-4">
        <h1 className="text-2xl font-bold items-center text-color-primary">{title}</h1>
          </div>
    )
}

export default Header